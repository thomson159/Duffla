import Zoom from 'react-medium-image-zoom';
import 'react-medium-image-zoom/dist/styles.css';

# Osiedle budynków jednorodzinnych Architektów

Zespół budynków mieszkalnych jednorodzinnych w zabudowie szeregowej i bliźniaczej.  
Adres: Bielsko-Biała  
Powierzchnia użytkowa budynków: 2 660 m²  
Powierzchnia działki: 11 882 m²  

## Wizualizacja

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '14px' }}>
  {Array.from({ length: 5 }, (_, i) => (
    <Zoom key={i}>
      <img alt="portfolio" src={`/compressed/jedno/archi/${i + 1}.jpg`} style={{ width: '100%', height: 'auto' }} />
    </Zoom>
  ))}
</div>

## Wnętrza

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(1, 1fr)', gap: '20px' }}>
  {Array.from({ length: 1 }, (_, i) => (
    <Zoom key={i}>
      <img alt="portfolio" src={`/compressed/jedno/archi/2/${i + 1}.jpg`} style={{ width: '100%', height: 'auto' }} />
    </Zoom>
  ))}
</div>

[więcej]( /projekty/wnetrza/os )
