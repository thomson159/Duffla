import Zoom from 'react-medium-image-zoom';
import 'react-medium-image-zoom/dist/styles.css';

# Dom jednorodzinny Meszna Świerkowa

Budynek mieszkalny jednorodzinny z garażem podziemnym.  
Adres: Meszna  
Powierzchnia użytkowa: ok. 225 m²  

## Wizualizacja

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '14px' }}>
  {[
    "1.jpg", "3.jpg", "4.jpg", "5.jpg", "7.jpg", "10.jpg", "11.jpg", "2.jpg", "12.jpg"
  ].map((file, index) => (
    <Zoom key={index}>
      <img alt="portfolio" src={`/compressed/jedno/swier/${file}`} style={{ width: '100%', height: 'auto' }} />
    </Zoom>
  ))}
</div>

## Wnętrze

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(1, 1fr)', gap: '20px' }}>
  {[
    "8.jpg"
  ].map((file, index) => (
    <Zoom key={index}>
      <img alt="portfolio" src={`/compressed/jedno/swier/${file}`} style={{ width: '100%', height: 'auto' }} />
    </Zoom>
  ))}
</div>

[więcej]( /projekty/wnetrza/swier )
