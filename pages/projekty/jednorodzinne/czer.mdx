import Zoom from 'react-medium-image-zoom'
import 'react-medium-image-zoom/dist/styles.css'

# CZERWONA

<br/>

{Array.from({ length: 4 }, (_, i) => (
  <>
    <Zoom>
      <img alt="portfolio" src={`/compressed/jedno/czer/${i + 1}.png`} />
    </Zoom>
    <br/>
  </>
))}
