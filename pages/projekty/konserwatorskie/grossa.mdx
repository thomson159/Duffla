import Zoom from 'react-medium-image-zoom'
import 'react-medium-image-zoom/dist/styles.css'

# Manufaktura Grossa

Adaptacja i rozbudowa dawnej Fabryki Grossa.  
Adres: Bielsko-Biała  
Powierzchnia użytkowa: 7 200 m²

<br/>

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '14px' }}>
  {Array.from({ length: 20 }, (_, i) => (
    <div key={i} style={{ }}>
      <Zoom>
        <img 
          alt="portfolio" 
          src={`/compressed/kon/${i + 1}.jpg`} 
          style={{ width: '100%', height: 'auto', display: 'block', margin: '0 auto', backgroundColor: 'white' }} 
        />
      </Zoom>
    </div>
  ))}
</div>
