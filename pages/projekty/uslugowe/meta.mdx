import Zoom from 'react-medium-image-zoom'
import 'react-medium-image-zoom/dist/styles.css'

# Budynek hotelu „META” w Szczyrku

Budynek składający się z części basenowej, saun, spa, sal sportowych, sal konferencyjnych oraz apartamentów.  
Adres: Szczyrk  
Powierzchnia użytkowa: 4 200 m²

## Wizualizacja

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '14px' }}>
  {[
    "1.jpg", "2.jpg", "3.jpg", 
    "7.jpg", "8.jpg", "9.jpg", "10.jpg", "11.jpg"
  ].map((file) => (
    <div key={file} style={{ }}>
      <Zoom>
        <img 
          alt="portfolio" 
          src={`/compressed/uslugowe/meta/${file}`} 
          style={{ width: '100%', height: 'auto', display: 'block', margin: '0 auto', backgroundColor: 'white' }} 
        />
      </Zoom>
    </div>
  ))}
</div>

## Rysunek

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '14px' }}>
  {[
    "4.png", "5.png", "6.png"
  ].map((file) => (
    <div key={file} style={{ }}>
      <Zoom>
        <img 
          alt="portfolio" 
          src={`/compressed/uslugowe/meta/${file}`} 
          style={{ width: '100%', height: 'auto', display: 'block', margin: '0 auto', backgroundColor: 'white' }} 
        />
      </Zoom>
    </div>
  ))}
</div>
