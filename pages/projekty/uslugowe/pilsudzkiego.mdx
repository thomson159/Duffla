import Zoom from 'react-medium-image-zoom'
import 'react-medium-image-zoom/dist/styles.css'

# Piłsudzkiego

## Wizualizacja

{[
  "1.jpg", "2.jpg", "3.jpg", "4.jpg"
].map((file) => (
  <>
    <br />
    <Zoom>
      <img 
        alt="portfolio" 
        src={`/compressed/uslugowe/pil/${file}`} 
        style={{ backgroundColor: 'white' }} 
      />
    </Zoom>
  </>
))}

## Rysunek

{[
  "5.png"
].map((file) => (
  <>
    <br />
    <Zoom>
      <img 
        alt="portfolio" 
        src={`/compressed/uslugowe/pil/${file}`} 
        style={{ backgroundColor: 'white' }} 
      />
    </Zoom>
  </>
))}
