import Zoom from 'react-medium-image-zoom'
import 'react-medium-image-zoom/dist/styles.css'

# Dom jednorodzinny Mazańcowice

Budynek mieszkalny jednorodzinny.  
Adres: Mazańcowice  
Powierzchnia użytkowa: ok. 263 m²  

<br/>

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(1, 1fr)', gap: '20px' }}>
  {[
    "9.jpg", "10.jpg", "11.jpg"
  ].map((file, index) => (
    <Zoom key={index}>
      <img alt="portfolio" src={`/compressed/jedno/dob/${file}`} style={{ width: '100%', height: 'auto' }} />
    </Zoom>
  ))}
</div>
